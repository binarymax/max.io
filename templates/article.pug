
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block ogtitle
  meta(property="og:title", content=page.title)
  meta(property="twitter:title", content=page.title)

block ogimage
  meta(property="og:image", content=tools.image(page.metadata.image,page.url))
  meta(property="twitter:image", content=tools.image(page.metadata.image,page.url))

block ogdescription
  meta(name="description", content=tools.description(page.metadata.description))
  meta(property="og:description", content=tools.description(page.metadata.description))
  meta(property="twitter:description", content=tools.description(page.metadata.description))

block header
  div.logo
    h1.author
        a(href="/")= locals.name
    h2= page.metadata.date
  h1= page.title

block content
  article.article
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) « Full blog


