extends layout

block append vars
  - bodyclass = 'article-detail'

block header
  div.logo
    h1.nav
      ul.nav-list
        li 
          a(href="https://max.io") MAX.IO
        li 
          a(href="https://flowercity.ai") flowercity.ai
        li 
          a(href="https://lumi.re") Lumi.RE
        li 
          a(href="https://linkedin.com/in/maxirwin") LinkedIn
        li 
          a(href="mailto:contact@max.io") Email
        li 
          a(href="https://github.com/binarymax") Github

  h1 Max Irwin

block content
  include author
  each article in articles
    article.article.intro
      if article.metadata.source=='external'
        - var ico = '/images/icons/'+article.metadata.external_favicon
        header
          p.date
            span= moment.utc(article.date).format('DD. MMMM YYYY')
          h2
            a(href=article.metadata.external_url,target=_blank)
              img(src=ico,style="height:24px")
              =article.title 
        section.content
          if article.intro.length > 0
            != typogr(article.intro).typogrify()
        p.more
          a(href=article.metadata.external_url,target=_blank)
            =article.metadata.external_hostname
            img(src='/images/external-link-2.svg',style='width:12px;height:12px;margin-top:2px;margin-left:5px;display:inline-block;')
      else
        header
          p.date
            span= moment.utc(article.date).format('DD. MMMM YYYY')
          h2
            a(href=article.url)= article.title
        section.content
          // != JSON.stringify(article.metadata,null,2)
          if article.metadata.description
            != article.metadata.description
          else if article.intro.length > 0
            != typogr(article.intro).typogrify()
          if article.hasMore
            p.more
              a(href=article.url) more

block prepend footer
  div.nav
    if prevPage
      a(href=prevPage.url) « Newer
    else
      a(href='/archive.html') « Archives
    if nextPage
      a(href=nextPage.url) Next page »



